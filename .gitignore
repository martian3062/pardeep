# Personal data — NEVER commit
data/

# Extracted identity: the persona card and Mind Model describe real people,
# relationships and private behaviour. Code that builds them is public; the
# artefacts are not.
src/twin/persona.md
src/twin/mind_model.json
*.gguf
*.env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local tooling
.kilocode/
.claude/settings.local.json

# Model / cache artifacts (live in E:\cache, but just in case)
*.ckpt
*.safetensors
*.pt
*.onnx

# OS
Thumbs.db
desktop.ini
